#pragma once

class WriteText {
public:
	static unsigned int level, score, lives, time, ja;
	static unsigned int numbers[10];

	static void writeEverything(int level, int score, int lives, int time);
};